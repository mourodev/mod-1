---
import Navbar from "./Navbar.astro";
import { Image } from "astro:assets";
import profile from "../assets/profile.jpg";
import { Icon } from "astro-icon/components";
---

<div class="container">
  <Navbar />

  <div class="profile">
    <Image
      src={profile}
      alt="Mouro - Desenvolvedor Front-end minha foto de perfil"
      height={100}
      format="avif"
      class={"profile-image"}
      loading={"eager"}
    />

    <div class="profile-info">
      <div>
        <h1 class="profile-name">
          Mouro <Icon
            name={"verified"}
            size={15}
            title="Verificado gratuitamente"
          />
        </h1>
        <p class="profile-job">Desenvolvedor Front-end</p>
      </div>
      <div class="profile-description">
        <p>
          Olá, eu sou Mouro, <span class="profile-job"
            >Desenvolvedor Front-end</span
          > comprometido em criar experiências digitais que realmente fazem a diferença.
          Para mim, tecnologia é o caminho para o sucesso, e eu coloco essa visão
          em prática em cada projeto. Se você quer elevar sua ideia a outro patamar,
          estou aqui para isso. Explore minha trajetória e meus trabalhos e vamos
          começar algo incrível juntos!
        </p>

        <span class="profile-location"
          ><Icon
            name={"location"}
            size={20}
            title="Localização de Mouro - RJ Brasil"
          />
          Rio de Janeiro - Brasil</span
        >
        <a href="/sobre-mim" class="link-profile">
          Conheça minha trajetória
          <Icon name="arrow" size={20} />
        </a>
      </div>
    </div>
  </div>
</div>

<style>
  .profile-info {
    display: grid;
    gap: var(--space-xs);
  }
  .profile-image {
    border-radius: 4px;
    margin-bottom: var(--space-s);
  }

  .profile-name {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    font-size: 1.6rem;
    -webkit-text-size-adjust: 100%;
    tab-size: 4;
  }
  .profile-job {
    color: var(--color-text-muted);
  }

  .profile-description {
    color: var(--color-text-body);

    & .profile-job {
      color: var(--color-text-light);
      font-weight: bold;
    }
  }

  .profile-location {
    display: flex;
    align-items: center;
    gap: 5px;
    margin-top: var(--space-s);
    font-weight: bold;
    font-size: 0.9rem;
    color: var(--color-text-muted);
  }

  .link-profile {
    color: inherit;
    text-decoration: none;
    font-weight: bold;
    display: inline-flex;
    align-items: center;
    gap: 5px;
    margin-top: var(--space-s);
    font-size: 0.9rem;
    background-color: var(--color-bg-card);
    padding: var(--space-xs) var(--space-m);
    border-radius: 4px;
    transition: all 0.2s ease;

    &:hover {
      background-color: var(--color-card-hover);
      transform: translateY(-5px);
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    }

    &:focus {
      color: var(--color-primary);
    }
  }
</style>
